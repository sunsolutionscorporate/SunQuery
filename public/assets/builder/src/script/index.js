!(function (a) {
   try {
      if (!window.sunQuery) throw new Error(`library 'sunQuery.js' belum terpasang`);
      return a(q);
   } catch (error) {
      console.warn(error);
   }
})(async function (n) {
   const log = console.log;

   // kode penutup

   new Kernel();
});